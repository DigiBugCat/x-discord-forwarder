services:
  x-discord-forwarder:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    # For test mode (TEST_TWEET_ID), use: docker compose up --build --no-deps --abort-on-container-exit
